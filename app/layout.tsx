import { Analytics } from "@vercel/analytics/next";
import type { Metadata } from "next";
import { Manrope } from "next/font/google";
import localFont from "next/font/local";
import "./globals.css";

import ClientRoot from "./components/ClientRoot";
import Navbar from "./components/Navbar";
import Footer from "./sections/Footer";

const richmond = localFont({
  src: [
    { path: "../public/fonts/Richmond-Display-Light.woff2" },
    { path: "../public/fonts/Richmond-Display-Light.woff" },
  ],
  variable: "--font-richmond",
  display: "swap",
});

const manrope = Manrope({
  subsets: ["latin"],
  variable: "--font-manrope",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Anuj Istwal | UI/UX Designer",
  description:
    "UI/UX Designer Anuj Istwal – crafting modern interfaces, motion design & micro-interactions.Generated by create next app",
  icons: {
    icon: "/images/favicon.png",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`${richmond.variable} ${manrope.variable} antialiased`}>
        <ClientRoot />
        <Analytics />

        <Navbar />
        {children}
        <Footer />
      </body>
    </html>
  );
}
